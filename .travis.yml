language: python

python:
  - "2.7"
  - "3.4"

install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq python-numpy python-scipy
  - pip install argparse pyyaml cython
  - pip install git+https://github.com/Theano/Theano.git@rel-0.7
  - pip install git+https://github.com/lisa-lab/pylearn2.git

script:
  - nosetests -v sknn
